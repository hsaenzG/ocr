# OCR

| Campo | Valor |
|-------|-------|
| `PUBLIC_API_BASE_URL` | URL del HTTP API (output `ApiUrl` tras `cdk deploy`) |

Copia a `.env` en `apps/web/` o exporta antes de `astro build` / `astro dev`.
